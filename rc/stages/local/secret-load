#!/bin/sh
# Push every secret in .env into the "local" SST stage via sst secret set.
# Wraps scripts/load-env-secrets.sh with STAGE=local.
set -eu
cd "$(dirname "$0")/../../.."
exec env STAGE=local ./scripts/load-env-secrets.sh "$@"
